@model DarwinCMS.WebAdmin.Areas.Admin.ViewModels.Users.EditUserViewModel

@{
    ViewData["Title"] = "Edit User";
}

<div class="container mt-4">
    <h2 class="text-center mb-4"><i class="fas fa-user-edit me-2"></i>Edit User</h2>

    <div class="mx-auto" style="max-width: 600px;">
        <form asp-action="Edit" method="post">
            <input type="hidden" asp-for="Id" />

            <div class="mb-3">
                <label asp-for="FirstName" class="form-label">First Name <span class="text-danger">*</span></label>
                <input asp-for="FirstName" class="form-control" required />
                <span asp-validation-for="FirstName" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="LastName" class="form-label">Last Name <span class="text-danger">*</span></label>
                <input asp-for="LastName" class="form-control" required />
                <span asp-validation-for="LastName" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Username" class="form-label">Username <span class="text-danger">*</span></label>
                <input asp-for="Username" class="form-control" required />
                <span asp-validation-for="Username" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Email" class="form-label">Email <span class="text-danger">*</span></label>
                <input asp-for="Email" class="form-control" required />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Gender" class="form-label">Gender <span class="text-danger">*</span></label>
                <select asp-for="Gender" class="form-select" required>
                    <option value="">Select Gender</option>
                    <option value="Male" selected="@("Male" == Model.Gender)">Male</option>
                    <option value="Female" selected="@("Female" == Model.Gender)">Female</option>
                    <option value="Other" selected="@("Other" == Model.Gender)">Other</option>
                </select>
                <span asp-validation-for="Gender" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="BirthDate" class="form-label">Birth Date <span class="text-danger">*</span></label>
                <input asp-for="BirthDate" class="form-control" type="date"
                       min="@DateTime.UtcNow.AddYears(-130).ToString("yyyy-MM-dd")"
                       max="@DateTime.UtcNow.ToString("yyyy-MM-dd")"
                       value="@Model.BirthDate.ToString("yyyy-MM-dd")"
                       required />
                <span asp-validation-for="BirthDate" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="MobilePhone" class="form-label">Mobile Phone</label>
                <input asp-for="MobilePhone" class="form-control" />
                <span asp-validation-for="MobilePhone" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="LanguageCode" class="form-label">Language Code <span class="text-danger">*</span></label>
                <input asp-for="LanguageCode" class="form-control" required />
                <span asp-validation-for="LanguageCode" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="ProfilePictureUrl" class="form-label">Profile Picture URL</label>
                <input asp-for="ProfilePictureUrl" class="form-control" />
                <span asp-validation-for="ProfilePictureUrl" class="text-danger"></span>
            </div>

            <div class="form-check mb-2">
                <input asp-for="IsEmailConfirmed" class="form-check-input" />
                <label asp-for="IsEmailConfirmed" class="form-check-label">Is Email Confirmed</label>
            </div>

            <div class="form-check mb-3">
                <input asp-for="IsMobileConfirmed" class="form-check-input" />
                <label asp-for="IsMobileConfirmed" class="form-check-label">Is Mobile Confirmed</label>
            </div>

            <div class="mb-4">
                <label asp-for="RoleIds" class="form-label">Roles <span class="text-danger">*</span></label>
                <select asp-for="RoleIds" class="form-select" asp-items="Model.Roles" multiple required></select>
                <span asp-validation-for="RoleIds" class="text-danger"></span>
            </div>

            <div class="d-flex justify-content-between">
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="fas fa-arrow-left me-1"></i> Back to List
                </a>
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save me-1"></i> Save Changes
                </button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
